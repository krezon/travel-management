apiVersion: v1
kind: Service
metadata:
  name: travel-management
spec:
  type: NodePort
  selector:
    app: travel-management
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
